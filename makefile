run:
	go run ./cmd/app

build:
	go build -o app .
# -o app - указывает имя выходного файла (app)
# . — значит «бери текущую директорию как модуль» (где есть go.mod).

start:
	./app

# ./ означает «в текущей директории».
# app — имя исполняемого файла, который ты собрал выше.